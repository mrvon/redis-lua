require "DB_KEY"

local handler = {}

handler[KEY_DIAMOND_INVEST_GLOBAL] = function(a_table, b_table)
    -- print(a_table and "a local_open_id: " .. a_table.local_open_id)
    -- print(b_table and "b local_open_id: " .. b_table.local_open_id)
    local n_table = {}
    return n_table
end

return handler
